# KingsLanding 项目规则

## 作用范围
- 除非更深层目录中存在新的 `AGENTS.md` 对局部规则进行覆盖，否则本文件规则适用于整个 `KingsLanding` 仓库。

## 存档兼容原则
- 手动试玩存档属于持续有效的用户数据，不视为可随意删除的测试产物。
- 默认要求：后续所有功能开发，必须尽量保持 `RandomGovernancePlaytest` 及相关地图试玩场景的现有手动存档可继续使用。
- 优先采用可热更新兼容的实现方式。除非技术上确实无法兼容，否则不得重置、删除、覆盖或判定现有手动试玩存档失效。
- 如果某次改动确实必须断档，必须先明确说明：
  - 为什么无法继续兼容旧存档；
  - 会影响哪些存档槽、场景入口或流程；
  - 是否存在迁移方案、降级方案或替代方案。
- 不允许静默引入存档格式破坏。

## 测试存档隔离原则
- 自动化测试、验证场景、临时脚本和调试工具，必须使用独立的测试存档路径。
- 测试代码不得读取、覆盖或删除 `RandomGovernancePlaytest` 使用的手动试玩存档路径。
- 测试中的任何 `delete_save()` 调用，只能作用于测试专用存档槽。
- 任何新增的验证场景或调试工具，只要需要持久化数据，都必须定义自己的独立存档路径。

## RandomGovernancePlaytest 规则
- `res://scenes/testScenes/RandomGovernancePlaytest.tscn` 视为持续使用的手动试玩入口场景。
- 新功能开发不得把该场景改成每次进入都强制重新生成地图，除非用户明确要求这样做。
- 不得无理由移除该场景已有的读档、恢复、已保存地图加载或兼容旧存档内容的能力。
- 修改加载、预热、保存、恢复、重置或流式生成逻辑时，必须尽量保持已有兼容存档可以直接重新打开，而不是重新走完整预热或完整加载。

## 存档格式变更原则
- 除非功能实现确有必要，否则不要修改存档结构或存档版本号。
- 如果必须修改存档结构，优先采用以下方式之一：
  - 向后兼容读取旧存档；
  - 在加载时执行明确的迁移逻辑；
  - 只增加字段，保持旧存档仍然有效。
- 只有在确实无解时，才允许采用强制失效旧存档的方式。

## 诊断与日志原则
- 当存档恢复失败时，优先输出明确的失败原因，不允许仅做静默回退。
- 只要修改了持久化相关逻辑，就必须清楚区分：
  - 手动试玩存档行为；
  - 自动化测试存档行为；
  - 一次性调试存档行为。

## 文档语言规则
- 本项目中所有新建的文字性文档，必须默认使用中文编写。
- 本项目中所有需要修改、补充或续写的已有文字性文档，修改后的新增内容也必须使用中文编写。
- 除专有名词、路径、代码标识符、接口名、文件名、引擎术语或确实无法自然翻译的特殊英文单词外，文档正文不得使用英文句子或英文段落。
- `AGENTS.md` 本身也必须使用中文编写。

## 代码注释规则
- 所有新增代码注释必须使用中文，且要直接说明该段代码的真实目的、约束、边界或原因。
- 修改已有代码时，如需新增注释或补充说明，也必须使用中文。
- 除接口名、类型名、路径、协议字段名等必须保留原文的技术标识外，注释正文不得使用整段英文。
- 不允许写无意义、空泛或仅重复代码表面的注释；注释必须真正帮助理解逻辑。

## 实施偏好
- 在本项目中实现任何新功能时，必须优先满足“尽量保持现有手动试玩存档持续可用”这一约束。
